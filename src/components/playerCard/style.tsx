const profileStyle = {
    margin: "1rem",
    display: "flex",
    justifyContent: "center"
}

const imgStyle = {
    width: "75px"
}

const infoStyle = {
    display: "inline-block",
    marginLeft: "12px",
}

const nameStyle = {
    fontWeight: "bold" as "bold",
    fontSize: "1.25rem"
}

const formStyle = {
    border: "thin solid #ddd",
    backgroundColor: "#fbfbfb",
    padding: "2rem",
    marginBottom: "2rem",
    display: "flex",
    justifyContent: "center"
}

const headerStyle = {
    textAlign: "center" as "center",
    fontSize: "1.5rem",
    marginBottom: "1rem"
}

export {
    profileStyle, imgStyle, infoStyle, nameStyle, formStyle, headerStyle
}